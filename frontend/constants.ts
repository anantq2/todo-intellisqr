export const API_DELAY_MS = 600; // Simulates network latency

export const STORAGE_KEYS = {
  USERS: 'taskflow_users',
  TODOS: 'taskflow_todos',
  LOGS: 'taskflow_logs',
  TOKEN: 'taskflow_token', // Stores the "JWT"
};